#!/usr/bin/env node
/**
 * DEPRECATED: TTL setup for soft-delete was removed when soft-delete support was removed.
 * This repository no longer uses a `deletedAt` TTL field because drafts are hard-deleted immediately.
 * Keep this file only for historical reference.
 */

console.log('This script is deprecated: soft-delete/TTL is no longer used in this project.');
process.exit(0);
